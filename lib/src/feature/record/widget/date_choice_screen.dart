import 'package:flutter/material.dart';

class DateChoiceScreen extends StatelessWidget {
  const DateChoiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
