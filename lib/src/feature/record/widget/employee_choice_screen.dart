import 'package:flutter/material.dart';

class MasterChoiceScreen extends StatelessWidget {
  const MasterChoiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
